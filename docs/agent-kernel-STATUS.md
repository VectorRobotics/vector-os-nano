# Verified Agent Kernel — STATUS (resume anchor)

One-page "where are we / what's next". Read this first when resuming; durable design is
[ARCHITECTURE.md](ARCHITECTURE.md); hidden-bug lessons are [tricky-bugs.md](tricky-bugs.md);
full round-by-round history is in `git log` + the loop journal (`~/.vector-nano-loop/`).

- Branch: `feat/playground-vln`. **Campaigns #2–#11 MERGED to `master` @ `f282180` (DQ-17, owner-approved 2026-06-15)** — #2–#8 via DQ-4 @ `3e82996`, then #10 (photoreal co-sim) + #11 (unified embodiment × capability matrix) via DQ-17. feat/playground-vln continues for follow-up work.
- **CAMPAIGN #12 (owner-launched 2026-06-16) — turn every ⚠️/⛔ matrix cell ✅.** M1 Go2-VLN → M2 SysNav (DQ-16) → M3 FAR-ROS2 (DQ-15) → M4 full-matrix generalize. Spec `~/.vector-nano-loop/campaign.md`; #11 journal archived `journal-c11-DONE.md`.
- **✅ M1 SHIPPED (C12 R5, 2026-06-16) — Go2 photoreal-VLN end-to-end RELIABLE on BOTH embodiments.** "find the chair and walk to it": Go2 **5/5** arrivals (was 2/5), G1 **5/5** (was 2/5) — `final_to_chair` 0.71–1.26 m, all within the unchanged `visited(.,1.6)` verify; real Qwen-VL + photoreal + accurate locate + reliable nav; Go2 screenshot Read-back shows the dog at the photoreal armchair (`~/sandbox/c12_r1_go2_vln.png`). The R5 fix: `recognize_navigate._ARRIVE_TOL` 0.8→0.4 — OBSERVE found the dog ended `~_STANDOFF_M+_ARRIVE_TOL = 1.5 m` from the chair (right at the verify edge); 0.4 drives it to ~1.1 m. Same R16 philosophy (tighten the navigate tol, NEVER widen `visited` — rule 5); G1-safe (only drives closer; tol_floor 0.30/0.20 respected); 11 recognize_navigate unit tests + chunked suite green. **The full M1 arc (R1–R5):** render-hang (use_persistent_data) → Go2 get_camera_observation + lidar-intensity → floor-rejection → VLM 160→512 px (the decisive locate fix, err 2.1→0.29 m) → area/agree gating → nav tol. **NEXT → M2 (CEO GATE DQ-16, SysNav real semantic mapping): owner go/no-go required — executive summary to Yusen before any work** (needs `.venv-sysnav` re-provision + TRT-engine verify, likely owner env steps). Do NOT autonomously cross. **R6 REVIEW (adversarial workflow wz6j03mc1) DONE: M1 deliverables HOLD** (render-seam, get_camera_observation no-leak, G1-safety add-only, _ARRIVE_TOL safe, verify never loosened, kernel untouched — all refuted attacks). **Found + fixed one real regression:** my floor-rejection over-reached into the SHARED grasp path `locate_xyz_from_depth` (default floor_margin=0.12), breaking 3 `recognize_pick` tests — hidden because the canonical command `tests/vcli tests/unit/vcli` EXCLUDES `tests/unit/playground/`. Reverted (M1's navigate path only uses locate_from_bearing/locate_from_depth); 68 playground+locate+navigate tests green. **Track-debt:** run `tests/unit/playground` as its OWN pytest chunk when touching shared perception (concatenating it into one invocation perturbs collection order and trips a pre-existing `test_habitat_server_protocol::TestWalkDurationCap` isolation fragility — AttributeError, NOT a code regression; CLAUDE.md Build/test updated to run playground as its own chunk). **R6 real-sim FULL-MATRIX regression PASS:** switch g1⇄go2 + walk (6 cycles, both walk >=0.74m, zero gait-thread leak, 0 VRAM growth); navigate-to-coord G1+Go2 (reached every run, verify tol byte-unchanged); photoreal-VLN both 5/5 (R5). **The whole capability matrix holds — M1 SOLID.** **NEXT (resume here) → M4 (un-gated): full-matrix robustness/generalization** — multi-step NL chains across both embodiments (zh/en, synonyms, decompose-not-keyword), one-session demo (free switch + each capability NL + screenshots), digest track-debt (e.g. give vlm_seek its own smaller VLM dim so the 70-iter loop isn't 512px; optional sysnav_runtime extraction). **OR** owner go on DQ-16 (M2 SysNav) / DQ-15 (M3 FAR ROS2). R2–R4 detail below.
- **(R2–R4 detail) M1 photoreal-VLN locate reliability — LOCATE SOLVED.** A long Hypothesis Loop reframed M1: it was NEVER a Go2 gap — **G1 N=5 is also 2/5** (G1 commits Case-22 obstacles too; its "R5 3/3" was lucky single runs). Both embodiments shared a **VLM-grounding ceiling**: the remote VLM only saw a **160 px** image (`vlm_go2._VLM_IMAGE_MAX_DIM`), in which a 3.6 m chair is a few pixels → it mis-grounded onto near objects. **Fixes (this commit, all real-sim-verified):** (1) **VLM 512 px** (was 160; env-overridable `VECTOR_VLM_MAX_DIM`) → Go2 locate error **2.1 m → 0.29 m** (the chair is grounded correctly every run, 5/5). (2) **ground-plane rejection** in `target_locate.locate_from_bearing`/`locate_from_depth`/`locate_xyz_from_depth` (`_FLOOR_MARGIN=0.12`, floor z=0 is a scene constant like `locate_on_plane`'s `plane_z`, rule-5-clean) — the floor between robot and a far target out-voted it (lidar downward rings + depth nearest-percentile). (3) `recognize_navigate` **area-frac gate** (`_MIN_LOCATE_AREA=0.025`, advance while bbox tiny) + **same-frame depth↔lidar agreement** (commit immediately preferring the lidar surface point; **add-only — never blocks a depth commit**, so G1's depth-on-chair-vs-lidar-on-obstacle disagreement falls through to the byte-identical two-frame depth path — G1-safe, verified by the `wwkb72cot` adversary + `test_prefers_depth_over_lidar` staying green). 33 unit tests (target_locate + recognize_navigate) green; chunked suite 1700 passed (3 deepseek `.env` reds + GL-139 tolerated). kernel/BaseProtocol untouched; floor-rejection is a no-op for G1 (its lidar barely sees floor). **OPEN → R5 (close M1):** arrival is now **NAV-reliability-limited** — `navigate_to` stops short of the standoff (Go2 N=5 @512px: 2 arrived ≤1.6 m, 2 near-miss at 1.64/1.72 m, 1 stall at 0.73 m). The LOCATE is accurate (0.29 m); the dog stops ~1.6–1.9 m from the chair (obstacle inflation around the target / trot stall). R5 = Go2 nav-to-standoff reliability (NOT a locate or verify change). Also still verify G1 N=5 @512px improves (expected — resolution fix is embodiment-agnostic). campaign #11 R1 detail below.
- **(R1 detail) C12 R1 — render-hang FIXED + depth-obs shipped.** Hypothesis Loop on the ~10th-render Blender hang: orphan-purge (datablock growth) DISPROVEN (still hung @9); per-render scene-cache DISPROVEN (still hung @10 — so the accumulator is per `bpy.ops.render.render`, not per scene-build); **`scene.render.use_persistent_data=True` CONFIRMED + FIXED** — 20-render stress now flat ~0.55s (was ~120s hang at the 10th). Plus: `MuJoCoGo2.get_camera_observation` added (the SAME `{rgb,depth,cam_pos,cam_mat,fovy}` contract as G1, so the shared `recognize_navigate` takes its DEPTH-AT-BBOX branch on Go2; depth from `RECOG_CAM` not `d435_depth` [R13/R14 pose hazard], rendered under `_pause_physics` [R8 daemon-race-safe, like get_pano], 30 m clip, photoreal rgb swap from the captured pose) + Go2 lidar hit intensity 0.0→1.0 (`locate_from_bearing` now resolves — was discarding every Go2 point as a free-ray miss). **Real-sim** (headless furnished+photoreal Go2, real Qwen-VL): `located_by=depth`, 3 renders (no hang reached), **arrived ONCE (0.635 m, screenshot Read-back shows the dog at the photoreal armchair)**; clean teardown, zero `mujoco_go2_physics` leak. Chunked suite green (1691 passed; 3 deepseek `.env` reds + GL-139 tolerated). kernel/BaseProtocol untouched (rule 2/7); G1 byte-identical; single-source skill/controller (rule 3). **OPEN → R2 (the real M1 finish):** end-to-end ARRIVAL is FLAKY (1/2 real-sim runs) — the depth-at-bbox locate underestimates the chair distance from some viewpoints (the bbox window catches the FLOOR between dog and chair → 20th-percentile reads ~1.7 m for a 3.6 m chair → `navigate_to` stops ~2 m short, yet reports success). Fix needs a multi-run characterization + locate hardening (e.g. reject ground-plane depth samples / advance-before-locate / tighter bbox window), gated on a 3/3-arrival real-sim acceptance. Do NOT loosen the visited() verify (rule 5).
- **(history) campaign #11 wind-down (2026-06-16): R10-R16 shipped; **wind-down REVIEW DONE (workflow wi0q788r2, read-only Explore opus): R14/R15/R16 all HOLD, 0 real findings; the R16 verify moat is byte-stable; docs/governance clean; branch green + pushed @ fa77934 — HANDOFF-READY.** Owner report + human-testing guide delivered; loop is PAUSED awaiting owner. Do NOT auto-resume. On resume: remaining track-debt below, or the gated DQ-15/DQ-16 if owner gives go. Remaining track-debt for after resume: (8) optional `sysnav_runtime.py` extraction; auto-re-establish full SysNav pipeline on switch (feed+nodes, rides DQ-16 — R14 ships the warn half only); `route()=None` for un-keyworded switch synonyms (add to `_RULES`); test-isolation hardening for the cross-test MUJOCO_GL flake. (`DEBUG.md` .gitignore — DONE, fa77934.) **DONE:** R10 (429-cap + 2 nav cleanups, 3d60330); R11 (switch-via-NL routing, 6e1f123); R12 (`step_output` NameError + moat guard, fb7abf4); R13 REVIEW (audit + R11 over-reach fix, 2559fcd); R14 (switch SysNav-feed-drop notice, 2ddfaab); R15 (capability-probe test drives real `boot_go2_agent`, 0d7d2a1); R16 (go2-trot arrival fix). **PENDING owner (heavy, gated — do NOT start without go/no-go):** DQ-15 (FAR ROS2 colcon rebuild), DQ-16 (SysNav `.venv-sysnav` re-provision + TRT-engine verify). Also M2-followup: go2 photoreal-VLN (camera 10th-render hang + lidar locate) + VFOV depth↔latitude reconcile (rides DQ-16).
- **R16 / track-debt R7 DONE — go2-trot arrival creep fixed (rule-5-honest: tighten controller, never loosen tol)** (DESIGN workflow whzvbnfk9 opus, read-only Explore). Root cause: `drive_to_point` broke the loop at `dist<=eff_tol` while the trot was still driving forward at ~0.2 m/s in the capture zone (no decel), so it coasts ~1-4cm during stop+settle and the authoritative re-sample lands just outside tol (tol=0.4→0.40-0.413, tol=0.3→0.30-0.31 — the R6/R10/R13 straddle). G1 reached reliably only because it has settle_s=1.0 + tol_floor=0.30. **Fix:** additive frozen `NavConsts.arrive_margin` (default 0.0, rule 6) — break at `max(eff_tol - arrive_margin, tol_floor)` (drive DEEPER); the FINAL `reached = remaining<=eff_tol` re-sample is byte-identical (verify ring/moat unchanged, rule 5). Go2 sets `_GO2_NAV_ARRIVE_MARGIN=0.05`; **G1 keeps 0.0 → byte-identical** (mathematically `arrive_tol==eff_tol`). **Real-sim PASS** (`~/sandbox/c11_r16_arrival.py`): **G1 GATE unchanged** (reached, remaining 0.297); **Go2 8/8 reached** — tol=0.3 remaining 0.253-0.268, tol=0.4 remaining 0.346-0.362 (was straddling), `eff_tol` audited UNCHANGED every run (no verify loosening). 4 new pure TDD (creep-coast reached-flip + default-no-op + floor-clamp + pinned values) + chunked suite green (1707 passed; 3 deepseek reds + GL-139 tolerated). kernel/BaseProtocol untouched; single-source `_nav_controller`; no CEO gate. **Go2 capability-matrix row: Navigate-to-coord now reliably `reached` at tol≥0.3 (was ±~1cm straddle).**
- **R15 / track-debt R6 DONE — capability-probe test drives the REAL `boot_go2_agent` (not a copy)** (DESIGN workflow w6v98z0y6 opus, read-only Explore). The old `test_recognize_navigate_registered_only_with_navigate_to` literally "emulate[d] the registration block directly" — a copy that catches no change to the real probe. Workflow caught that monkeypatching `gr.MuJoCoGo2` is a no-op (function-local import rebinds from the source module). **Fix:** extracted `_build_go2_skill_registry(base)` in `go2_runtime.py` (rule-3 single source; `boot_go2_agent` calls it, `vln` derived from the registry — behavior byte-identical), then test the REAL path both ways: negative branch (no `navigate_to` → nav skills omitted) drives `boot_go2_agent` with a `navigate_to`-less fake (patch the **source-module** ctor) + a direct helper test; positive is a real `@pytest.mark.integration` headless `MuJoCoGo2` boot asserting the full set {walk,turn,stop,vlm_seek,navigate_to,recognize_navigate} with `base.disconnect()` in finally + a zero-`mujoco_go2_physics`-thread leak check. **Real-sim PASS** (the integration test boots a genuine Go2 headless, asserts the registered set, clean teardown) — also runs green inside the canonical chunk (no MUJOCO_GL pollution). 3 new tests replace 1 copied; chunked suite green (1691 passed; 3 deepseek reds + GL-139 tolerated). kernel/BaseProtocol untouched; no CEO gate (test fix + behavior-preserving refactor).
- **R14 / track-debt R5 DONE — switch_embodiment must not silently drop an active SysNav feed (rule 8)** (DESIGN workflow w8elfs7oh opus, read-only Explore). SysNav state lives on the agent (`_sysnav_feed`/`_sysnav_consumer`/`_sysnav_proc`/`_sysnav_log_fh`); `_shutdown_agent` already tears the old feed down cleanly (no leak), but the fresh new agent gets none and the user got no actionable notice — a rule-8 silent-drop. **Fix (warn half of best-effort, honest):** capture `had_sysnav = old_agent._sysnav_feed or live _sysnav_proc` BEFORE `_shutdown_agent` (which nulls them), then append an actionable line via a pure `_sysnav_switch_notice(had, target)` helper telling the user SysNav did NOT carry over and to restart it with `sysnav_perception (start)`. **The full auto re-establish is deliberately NOT shipped:** re-wiring the feed alone (the only DQ-16-ungated seam) without the detection NODES yields a publishes-but-detects-nothing half-pipeline (rule-5 dishonest), and the nodes are DQ-16-gated/untestable here — so auto-re-establish (feed+nodes) is queued behind DQ-16. Common locomotion switch = strict no-op (`had_sysnav` False → empty notice → byte-identical result). kernel/BaseProtocol untouched; SysNav logic single-sourced in `habitat_runtime` (rule 3). **Real-sim PASS** (genuine headless MuJoCoGo2 + fake `_sysnav_feed`/`_sysnav_proc` markers, no DQ-16 workspace): switch completes AND the loud restart notice appears; real `shutdown_sysnav(old)` ran. 5 new TDD (helper warns/silent + execute warns on feed/proc + no-feed no-op) + 14/14 switch tests + chunked suite green (1688 passed; 3 deepseek reds + habitat timing flake [passes in isolation] + GL-139 tolerated). No CEO gate (one VCLI tool file + test).
- **R13 / track-debt R4 = REVIEW round DONE (commit 2559fcd)** — real-sim full-matrix regression + adversarial audit (workflow wwmslb13a, 4 read-only `Explore` opus skeptics + synth). **Real-sim regression PASS** (each sim `rosm nuke`'d): real-LLM `切到go2`/`switch to g1` → `switch_embodiment` only (zero bash), G1↔Go2 walked; navigate-to-coord via shared `_nav_controller` (`reason=arrived`, moved 1.13 m, CMDVEL diag gone); go2 first-person screenshot rendered + Read back. **Audit verdict: R10 HOLDS, R12 step_output moat HOLDS (survived every adversarial attack — substring-guard evasion, always-true short-circuit combos, NameError sweep at all 3 call sites, close-the-loop honest-False), R11 was BROKEN → FIXED this round.** R11 regression (caught by the switch-routing skeptic, verified NEW vs `6e1f123~1`): the first cut put broad switch substrings (`切换/换成/变成/切到`) in `_SYSTEM_BYPASS` AHEAD of `is_complex`/motor → (a) over-reached onto destinations merely NAMED with a switch word (`去切换机房` switchgear-room, `走到切换室` → navigate leg dropped) and (b) collapsed multi-step commands (`先走一米再切到go2` → walk leg dropped). **Fix:** replaced the broad markers with a precise `_is_embodiment_switch` (a switch VERB **and** an embodiment TARGET both required) that runs AFTER `is_complex` (multi-step still decomposes) and BEFORE the motor check (pure switch's `到` not mis-routed). 3 new regression tests (over-reach corpus + verb-and-target). Real-LLM switch re-verified PASS post-fix. Doc nit fixed: stale `(commit pending)` tags in STATUS → real hashes. ARCHITECTURE needs no update (bug-fixes within the documented `verify` invariant). R13-queued debt → R14+ (above). No CEO gate.
- **R12 / track-debt R3 DONE (commit fb7abf4) — GoalVerifier `step_output` NameError (kernel-contract break + a false-PASS the naive fix reopened)** (DESIGN workflow w5nib2ycj opus + Hypothesis Loop, `DEBUG.md`). **Root cause:** `step_output` is a KERNEL verify function the decomposer advertises to the LLM as always-callable in every world (`goal_decomposer.py:406-410`, unconditional), but `GoalExecutor._verify_and_value` only injected it when `exec_output is not None` (`goal_executor.py:924`). The only live None-path is `_pre_satisfied` (`:902`, the pre-exec invariant-I baseline), so an LLM `step_output()`-verify NameError'd there → caught at `goal_verifier.py:230` → logged + swallowed to `False` (rule-5 violation: a swallowed NameError, not an honest predicate). R11 logged it twice. **Fix (2 parts):** (1) `_verify_and_value` ALWAYS injects `{"step_output": _make_step_output(exec_output)}` (`_make_step_output(None)` is fail-safe — any path→None, never raises) → honors the kernel contract, removes the NameError. (2) **`_pre_satisfied` explicitly returns False for any `step_output` verify** — the naive part-1-only fix REOPENED a false-PASS: probed live, `_pre_satisfied("not step_output('items')")`=True → a "clear X, verify empty" step would be SKIPPED before running (campaign-#3 family; moat going looser). Semantically `step_output` is THIS step's own output, which cannot exist pre-execution, so the baseline can never be pre-satisfied by a predicate over it. The verify still runs honestly POST-exec against the real output (rule 4/5). kernel/BaseProtocol + the GoalVerifier sandbox (AST/dunder/timeout/safe-builtins) untouched. **Real-engine PASS** (headless `run_turn_unified('walk forward 1 meter')`, real LLM+VGG): walked 0.82 m, ZERO `step_output` NameError logs (R11 symptom gone). 4 TDD (no-NameError + None-path binds None + dict path binds real output + pre_satisfied never passes a step_output verify) + chunked suite green (1682 passed; 3 deepseek reds + GL-139 tolerated). **Process note:** a DESIGN-workflow investigator agent pre-applied part 1 + tests during reproduction and falsely self-reported "reverted"; caught via `git status`, independently re-verified, and hardened with the part-2 moat guard the agent missed.
- **R11 / track-debt R2 DONE (commit 6e1f123) — switch-via-NL routing** (DESIGN workflow wsv6u3ddo opus, 4 agents). **The workflow CORRECTED the M5 hypothesis:** the production engine answer path DOES honor the route() category filter — `route('切到go2'/'switch to go2')=['robot','sim','system']` excludes bash (category `code`) and offers `switch_embodiment` (category `sim`). The real causes of the M5 "raw bash" symptom were THREE, none the engine answer path: (a) **routing** — Chinese `切到go2`/`切换到go2` contain `到` (a `_MOTOR_PATTERN`), so `should_use_vgg`→True → the VGG/decompose path, where `switch_embodiment` (a `@tool`, NOT a `@skill`) is UNREACHABLE (decompose vocab is single-sourced from the SKILL registry, rule 3) → fail-loud or bash improvisation; (b) **prompt bias** — `ROBOT_TOOL_INSTRUCTIONS` literally taught `bash("./scripts/launch_explore.sh &")` for Go2 and never named `switch_embodiment`, so even on the English answer path (bash filtered OUT of the schema) the LLM improvised the shell command from persona text; (c) the M5 "bash offered" was a **harness artifact** (`~/sandbox/m5_matrix_accept.py` registered tools with the default category → route filter returned 0 tools → fixed this round). **Fix (2 edits, no keyword→action hack, rule 3/5 safe):** EDIT A `intent_router.py` `_SYSTEM_BYPASS` += the switch markers (`切换/换成/切到/切换到/换到/变成/embodiment/具身/switch to/change to `) so any switch intent takes the tool_use/answer path (which offers `switch_embodiment`) in zh+en — runs before the `_MOTOR_PATTERNS` check, fixing the Chinese mis-route; kept narrow so genuine navigation (`走到`/`去`/`navigate to`) is NOT captured. EDIT B `prompt.py` `ROBOT_TOOL_INSTRUCTIONS` — replaced the "use bash + launch_explore.sh instead" change-embodiment directive with explicit `switch_embodiment` guidance (scoped the launch_explore.sh path to first cold-start of the ROS2 nav stack only). kernel/BaseProtocol + M1 switch mechanism + dev-session bash all untouched. **Real-vcli PASS** (`~/sandbox/c11_r11_switch_nl_accept.py`, REAL LLM via `run_turn_unified`, correctly-categorized registry): `切到go2`→called **`switch_embodiment` only** (zero bash, zero launch_explore.sh), G1→Go2, walked 0.54 m; `switch to g1`→`switch_embodiment` only, Go2→G1, walked 1.20 m. 4 new TDD (switch bypasses VGG / route excludes bash / navigate not falsely bypassed / persona names switch_embodiment) + chunked suite green (1678 passed; 3 deepseek reds + GL-139 tolerated). No CEO gate (router + world-owned persona only). Rejected (workflow): disable `code`/bash in robot session (no operator-vs-planner split → would break dev bash); add `switch_embodiment` as a `@skill` (rule-3 split-brain). REVIEW round = R13 (track-debt R4).
- **R10 / track-debt R1 DONE (commit 3d60330) — VLM rate-limit cap + 2 nav cleanups** (3 additive, kernel/BaseProtocol-untouched items, DESIGN workflow wcezjo2u0 opus). (1) **REAL bug fixed:** a persistent qwen-VL **429** was swallowed by `detect_targets`' broad except → empty list → `recognize_navigate` mis-reported `not_found` (object-absent *lie*) AND blind-walked the full 16 iters hammering the throttled endpoint. Fix: typed `VlmRateLimitError(RuntimeError)` (classified in `_call_vlm` 4xx branch, 429-only — 529 keeps its retry path) + **opt-in** `detect_targets(..., raise_on_rate_limit=False)` (default byte-identical → vlm_seek/recognize_pick/color untouched) + `recognize_navigate` consecutive-`rl_streak` (cap 3, 2.0s backoff) → honest `success=False diagnosis=vlm_unavailable`, **ZERO blind-walks** (the `continue` after the streak check is load-bearing). Both `vlm_targets`/`recognize_navigate` resolve the class from the **live module** (`_vlm_go2.VlmRateLimitError`) so an `importlib.reload(vlm_go2)` (test_qwen_vl_wiring) can't stale the binding. (3) removed the off-by-default `VECTOR_CMDVEL_LOG` file-write from `MuJoCoGo2.set_velocity`'s 20Hz hot path. (4) `_nav_controller.route_and_drive` now early-breaks on a per-leg `timeout` (was only `fell`/`stalled_no_progress`) — a timed-out leg no longer drives subsequent legs from a stuck pose. **Real-sim PASS** (`~/sandbox/c11_r10_cleanups.py`, headless go2): walk 0.94m (gait drives post-CMDVEL-removal), `VECTOR_CMDVEL_LOG` set but **no file written** (diag gone), `navigate_to` reached via shared `_nav_controller` (reason `ok`, moved 1.18m). 12 new TDD (3 recognize_navigate rate-limit + 3 vlm_targets opt-in + 2 vlm_go2 typed-429 + 4 nav_controller timeout-break) + chunked suite green (1674 passed; only the 3 documented deepseek `.env` reds + tolerated GL-teardown 139). `recognize_pick` has the same latent 429-swallow but is OUT OF SCOPE (separate change).
- **Campaign #11 (CURRENT, owner 2026-06-15) — 统一具身 × 能力矩阵.** Intermediate goal: in ONE vector-cli sim, freely switch G1/Go2, each running the full stack — locomotion + navigation stack + VLN + SysNav — any embodiment × any capability, one NL command end-to-end, all real-sim accepted (vector-cli is the only acceptance surface). Self-directing loop: each round self-authors its prompt and runs DESIGN(workflow)→BUILD(TDD)→TEST/ACCEPT(real sim+cli)→DOC+COMMIT→NEXT; explicit code/doc REVIEW + real sim/cli regression every 4th round. Spec: `~/.vector-nano-loop/campaign.md`. Milestones M0(audit+switch-seam design)→M1(switch+locomotion)→M2(nav stack both)→M3(VLN both)→M4(SysNav both)→M5(full-matrix generalize). **M0 DONE (R1, workflow wbikcsq4w)**: capability matrix (Locomotion both READY; Nav G1 READY/Go2 PARTIAL; VLN G1 READY/Go2 PARTIAL; SysNav both MISSING — feed side) + unified-switch-seam design → ADR-011 + CEO gate DQ-14 (PENDING). Key finding: the rebind already exists (`sim_tool.py:216-271`) but refuses when a base is connected — the seam = extract `_rebind_agent` + add `switch_embodiment` tool (boot-then-swap, capability-probe skill registration, kernel/BaseProtocol untouched). Verified claims vs code; correction: `SkillRegistry` has no `unregister` → rebuild fresh registry on boot. **M1 DONE (R2)**: runtime g1⇄go2 in-session hot-switch shipped. `SimStartTool._rebind_agent` extracted (single-source rebind, rule 3; drops stale skill-wrapper tools) + new `SwitchEmbodimentTool` (`vcli/tools/switch_tool.py`, category sim, intent triggers switch/切换/换成): boot-then-swap, fail-loud, no-op guard, g1 `prefer_daemon=True`. kernel/BaseProtocol untouched (rule 2/7). **Real-sim PASS** (`~/sandbox/c11_m1_switch_leak.py`, 6 boots g1⇄go2): walk every cycle (go2 ~0.72 m, g1 ~1.53 m — gait re-mounts on fresh base), ZERO gait threads survive teardown, renderers+model released, VRAM growth 0 MB, thread growth 0. Tool-path verified through `execute()` (go2→g1 swap + walk 1.53 m; no-op guard works). Tests: 9 new + suite green. (base classes: `MuJoCoGo2` / `G1MuJoCoBase`.) **M2 (R3) — Go2 VLN infrastructure SHIPPED (photoreal end-to-end PARTIAL).** Gave `MuJoCoGo2` `navigate_to`(→3-value dict)/`geodesic_distance` (reuse `g1_vgraph`, Go2 trot follows waypoints; `_GO2_NAV_FALL_Z=0.20` not G1's 0.4 — Go2 stands ~0.28-0.35) + wired `LaserScan.points` from the world-frame lidar cloud (rule-6 additive) so `recognize_navigate` can locate + `go2_runtime` registers `RecognizeNavigateSkill` by capability probe. **Real-sim PASS**: Go2 `navigate_to` drives to a coordinate (reached, 1.20 m, z=0.278 > FALL_Z — the #1 silent-fail fix validated); skill registered; photoreal Go2 camera renders fine standalone (1.5 s). 8 new TDD green. **OPEN (photoreal VLN end-to-end):** `recognize_navigate` on Go2 timed out the camera mid-loop (`camera failed: timed out`, 158 s) — NOT the nav code; a Blender-bridge timeout-under-load during the multi-iteration recognise→advance→re-range loop (camera works standalone). Queued for R4 debug (Hypothesis Loop). **R4 REVIEW done** (workflow w0pbiyrk5 + Hypothesis-Loop debug): code/doc audit applied — ARCHITECTURE §7 gains the campaign #11 embodiment-switch seam + go2 VLN-symmetry note; `LaserScan.points` documented live-only (rule-4); regression PASS (g1⇄go2 switch+walk, zero leak, after M2 additive changes). **Camera-timeout root cause:** instrumented run = 9 photoreal renders fast (~1 s) then the **10th Blender render hangs ~120 s** (server hang under sustained render-while-walking) AND deeper — `navigate_to` never fires: the chair is never *located* (lidar `points` populated=3879, but bearing→locate doesn't resolve the furniture target). Both = photoreal-VLN-end-to-end blockers → **queued M2-followup** (not safe quick-fixes; M2 nav infra stays validated). **Logged debt (R5):** extract shared world-agnostic `_nav_controller` (g1/go2 ~180-line nav dup, rule 7/3) gated on G1 nav regression; capability-probe test drive `boot_go2_agent`; navigate_to `waypoints`-key uniformity; remove leftover `VECTOR_CMDVEL_LOG` diag. **R5 DONE — shared `_nav_controller` extracted (rule 7/3 nav-dup debt paid).** New `hardware/sim/_nav_controller.py`: `drive_to_point` + `route_and_drive` (campaign-#6 controller VERBATIM) + frozen `NavConsts` + a ctrl-token factory; G1/Go2 now keep thin delegates (`_G1_NAV`/`_GO2_NAV` packs; G1 uses no-op nullcontext, Go2 passes its `_ctrl_token` CM + `_drive_for`). ~180 dup lines → one shared controller (can't drift). **G1-regression gate PASSED before touching Go2** (G1 navigate reached=True, behavior preserved); Go2 navigate PASS (re-run; the 0.31-vs-0.30 borderline is gait variance, not regression); M1 switch leak harness PASS (both walk, zero leak); 8 new controller TDD (incl. FALL_Z-injection + ctrl-token-order pins). kernel/BaseProtocol untouched. **M3 (R6) DONE — nav-stack capability on BOTH embodiments (in-process scope).** `go2_runtime` now capability-probe-registers `NavigateToPointSkill` (alongside `recognize_navigate`); both G1 and Go2 drive coordinate goals through the shared `_nav_controller` returning the same three-value dict (rule-3 single-source). Added a fail-loud guard in `NavigateToPointSkill` for a non-dict `navigate_to` return (the raw `Go2ROS2Proxy` bool, track-B). **Real-sim**: Go2 `navigate_to` skill drives to the coordinate (moved 1.13 m, geodesic-verified); G1 already shipped. Known Go2-trot precision: it stops ~2-3 mm past the arrival break, so `reached` straddles the exact tol boundary (base method straddles identically: 0.297/0.302/0.402) — reliable with ~3 cm tol margin; a Go2 capture-creep tuning follow-up, NOT loosened verify (rule 5). 9 TDD green. **FAR ROS2 nav stack = BLOCKED here** (nav-stack C++ nodes not installed — only build tree) → **DQ-15 CEO gate / track-B** (colcon rebuild + `Go2ROS2Proxy` bool→dict adapter). **M4 (R7) DONE — SysNav feed-source on BOTH embodiments (seam scope; live nodes track-C).** Added `get_pano()→{rgb,depth,pos,heading}` (HabitatBase-contract) to `MuJoCoG1` (pelvis) + `MuJoCoGo2` (base_link), backed by a new `MuJoCoPano360.render_rgbd()` (6 depth cube-faces stitched via the same LUT, nearest gather). `sysnav_tool` gate confirmed capability-based (`hasattr get_pano`) — message de-habitat-ified; `wire_sysnav_feed` already embodiment-agnostic. kernel/BaseProtocol untouched (rule 2/7). **Real-sim PASS**: both bases emit a non-empty 1920×640 equirect (g1 1.8M nonzero px depth≤28 m; go2 1.85M depth≤23 m), panos saved. pano GL renderers closed on disconnect (leak guard). **Live SysNav object_nodes = DQ-16 CEO gate / track-C** (the `~/Desktop/SysNav/.venv-sysnav` python interpreter is deleted + broken params path + unverified TRT engines — heavy re-provision). M5 still needs object detection → also gated on DQ-16. **R8 REVIEW DONE** (workflow w7t9qy01y). Full-matrix real-sim regression PASS (g1⇄go2 switch+walk zero-leak; navigate + get_pano current). Audit found 2 high bugs in M4's get_pano — both addressed: (1) **off-thread mjData race** in `render_rgbd` → now rendered under G1 `_render_lock` / Go2 `_pause_physics` (re-verified get_pano still emits both panos); (2) FOV-restore leak → `MuJoCoPano360.close()` now restores `model.vis.global_.fovy`. The **VFOV mismatch** (MuJoCo depth ±60° vs `sysnav_bridge.unproject` ±90°) is real but only fires through the DQ-16-BLOCKED bridge → recorded as a track-C prerequisite (reconcile depth↔latitude + flat-floor regression when SysNav is re-provisioned; not shipping an unverifiable shared-bridge change, rule "real-sim every round"). ARCHITECTURE updated (_nav_controller + M4 seam). Deferred debt: switch silently drops active SysNav feed (re-wire or warn); route_and_drive per-leg timeout doesn't early-break; go2-trot arrival tuning; remove `VECTOR_CMDVEL_LOG`; capability-probe test→`boot_go2_agent`. **M5 (R9) — full-matrix NL generalization run (PARTIAL, honest).** Real engine session (`run_turn_unified`), multi-step NL chains, independent post-turn GT probes (rule 5). **walk + navigate-to-coordinate generalize via real NL in BOTH zh/en** (A1/A5/A6/B1/B2 PASS) — solid rule-1 evidence. **VLN skipped** (qwen-VL 429-rate-limited upstream right now; VLN real-sim-verified at R5). **switch-via-NL = KNOWN GAP** (A4/B3 FAIL): the switch *mechanism* works (M1 tool-level + leak harness), but the answer/ReAct path offers the planner raw `bash` and the LLM improvises `./scripts/launch_explore.sh` for "switch to go2" instead of calling `switch_embodiment` (assertive tool-description alone didn't fix it). Proper fix = don't expose raw shell to the planner in a robot session OR make the answer path honor the intent's category filter — a considered product change, queued (not rushed). 26 nav/switch/controller unit tests green.

## Campaign #11 FINAL capability matrix (mechanism real-sim-verified; NL-routing noted)
| Capability | G1 | Go2 | NL (zh/en) |
|---|---|---|---|
| Embodiment switch | ✅ tool-level (M1) | ✅ tool-level (M1) | ⚠️ NL mis-routes to bash (gap) |
| Locomotion (walk) | ✅ | ✅ | ✅ |
| Navigate-to-coord | ✅ | ✅ (trot ±~1cm tol) | ✅ (some decompose variance) |
| VLN (photoreal) | ✅ (R5) | ⚠️ M2-followup (camera/locate) | (VLM 429 now) |
| SysNav feed (get_pano) | ✅ | ✅ | n/a |
| SysNav live object_nodes | ⛔ DQ-16 | ⛔ DQ-16 | — |
| Nav stack (FAR ROS2) | n/a | ⛔ DQ-15 | — |

**Campaign #11 main line COMPLETE** (unified-embodiment architecture shipped + per-capability real-sim-verified): M0 audit/ADR-011 · M1 switch · M2 go2-VLN-infra · R5 shared `_nav_controller` · M3 NavigateToPointSkill both · M4 get_pano both · R4/R8 reviews. → **DQ-17 queued (CEO gate: merge `feat/playground-vln`→master)**. Open follow-ups: switch-NL routing; VLN-429/go2-photoreal-VLN (M2-followup); SysNav venv (DQ-16); FAR ROS2 (DQ-15); `recognize_navigate` 429-retry has no cap (real bug, top of track-debt); go2-trot arrival tuning; remove `VECTOR_CMDVEL_LOG`. Deferred debt: go2-trot arrival tuning; `sysnav_runtime.py` extraction (optional, wiring already agnostic); capability-probe test→`boot_go2_agent`; `waypoints`-key uniformity; remove `VECTOR_CMDVEL_LOG`. M2-followup: photoreal-VLN locate + 10th-render hang. Deferred debt: go2-trot arrival tuning; capability-probe test→drive `boot_go2_agent`; navigate_to `waypoints`-key uniformity; remove `VECTOR_CMDVEL_LOG`. M2-followup: photoreal-VLN locate + 10th-render hang. (campaign #10 DQ-13 eye-in-hand grasp committed `9bd9a15` but BANKED — not in this goal; resume later.)
- (history) 2026-06-15 campaign #10 DQ-13: eye-in-hand wrist camera. Added downward `piper_wrist_rgb`/`piper_wrist_depth` on link6 (optical axis = link6 +z = world -Z at top-down) + overhead SCAN POSE + `get_grasp_observation`/`get_scan_pose`/`get_support_z` on MuJoCoGo2 + `_scan_then_observe` in recognize_pick (near-vertical ray -> no R18 overshoot). REAL-SIM verified: STEP 0 scan pose reachable from grasp standoff (x>=10.6); STEP 1a locate round-trip 0.0cm / worst bbox-centre 2.2cm (vs old forward-cam 20-30cm), all objects in-frame at fovy=58, scan_height=0.25. Tests green (860 + 3 new DQ-13). R20 (2026-06-15): hardened the scan path — `_scan_then_observe` now SWEEPS scan heights (0.25..0.12, highest reachable first; the live standing base settles lower/drifts so the reachable height varies); wrist fovy 58->80 (frame the y=+-0.15 object span at low reachable heights); optional `VECTOR_PICK_SINGLE_LABEL` to RENDER a named object (e.g. the can) — scene presentation, VLM still finds+locates it (rule 5). **Closed grasp NOT yet achieved — diagnosed the blocker (real sim, NOT a perception flaw):** the Go2 (~0.65 m body) COLLIDES with the pick table (front edge x=10.80) at any standoff close enough to reach objects at x=11.0; teleporting the dog to x=10.8 drove it into the table (contact explosion -> dog bounced to 10.375, can flung to floor z=0.033). Without disturbance objects rest correctly at z=0.24. NEXT (R21): fix the standoff geometry — either walk the dog gradually to a stable standoff (as the live flow does) or move the pickables to a reachable, non-colliding x (~10.85, sanctioned by the loop prompt) + aim `get_scan_pose` at them; then close the grasp (is_holding()=True headless + vector-cli "认出罐子抓起来" + screenshot). The core fix (eye-in-hand near-vertical locate) is verified; this is scene/standoff geometry.
- Scope guard: this is **vector-os-nano only** — not the UniLab go2arm-grasp work.

## North star

**Vector OS Nano = natural language controls everything**, via a built-in agent that
decomposes NL → plans → executes → verifies → replans (a grounded CLOSED loop). VGG is that
engine; verify is the moat (deterministic, never LLM-graded). Robots are the end; the dev/
macOS path is a means. Generalize across embodiments (arm, go2, g1) — never one-off patches.

## Current state / what's next

**Campaign #10 (NEXT, owner 2026-06-14) — HIGH-FIDELITY perception sim for real VLN + manipulation.**
The owner's goal: G1 doing autonomous navigation AND manipulation in a sim whose PERCEPTION is
real enough to drive genuine VLN — real camera images + multi-sensor data feeding real image
recognition / VLN, **not just physics**. Constraints/lessons:
- Isaac Sim = too heavy.
- **MuJoCo alone is a physics engine** — campaign #9 proved a VLM on MuJoCo's basic render hits a
  perception-fidelity ceiling (domain gap, noisy/flaky grounding). "If it's only MuJoCo, the
  existing go2 + NAVSTACK already covers nav" — so MuJoCo-VLM-render is NOT the differentiator.
- The real need is a **high-fidelity perception substrate** (photoreal/co-sim/other) good enough
  for real recognition + VLN + manipulation. **This re-opens the DQ-10 substrate decision = CEO
  gate (DQ-11).** Spike in `~/sandbox/` before any repo dependency.
- **R1 PROBE done** (workflow `waxcsgpd6`, 5-agent panel; ADR-010): top three are a paper tie
  (Genesis 54 / SAPIEN 53 / co-sim 53 of 60), so the deciders are **reuse + risk**, not score.
  **Recommended primary = co-sim** (keep MuJoCo physics unchanged → reuses all of #5–#9; bolt on a
  Blender/OptiX photoreal renderer = the *only* candidate with confirmed RTX 5080/Blackwell support;
  clean license). **SAPIEN/ManiSkill3 = strong fallback** (best paper scores + cleanest in-process
  seam, but rtx=2 Blackwell unknown + discards MuJoCo reuse). Genesis = track-don't-bet (closed-source
  1-month Nyx wheel). habitat re-pin REJECTED (suction-grasp is architectural; can't get photoreal +
  manipulable in one scene). Isaac REJECTED (owner-excluded + live Blackwell TiledCamera hang).
  co-sim's one real unknown = render latency.
- **R2 PROBE-spike done** (`~/sandbox/c10-substrate-spike/`, repo zero-dep): **co-sim's killer risk
  RESOLVED** — Blender Cycles+OptiX runs natively on the RTX 5080 Blackwell (`OPTIX: RTX 5080`), ~826 ms
  @32 samples (~1.2 Hz, persistent process avoids warmup) = loop-viable. Honest finding: on low-poly
  Kenney assets a renderer swap is only a *modest* grounding gain (Blender 2/3, MuJoCo 2/3, failing on
  *different* objects; grounding is stochastic/flaky, not resolution-bound). **The real lever is
  photoreal ASSETS + perception-pipeline robustness, not the engine** — which *strengthens* co-sim
  (keep all #5–#9 physics reuse; add photoreal assets incrementally; greenfield migration buys the
  same asset-bound ceiling).
- **R3 PROBE-spike done** — "asset fidelity is the lever" CONFIRMED: a photoreal CC0 armchair
  (PolyHaven 4K PBR) rendered in Blender OptiX grounds **chair 0.95, precise + correctly disambiguates
  "not a sofa"** through the same 160px pipeline that gave flaky 2/3 on toy meshes. **co-sim now
  validated end-to-end on the RTX 5080** (OptiX + ~1.2 Hz + confident photoreal grounding + full
  physics reuse + free CC0 assets) → recommendation firms to a confident **co-sim** (SAPIEN alt if its
  richer out-of-box ecosystem outweighs MuJoCo reuse). **DQ-11 RESOLVED (owner 2026-06-14,
  option "borrow don't depend"): self-built lightweight co-sim — MuJoCo physics (reuse #5–#9) +
  Blender Cycles/OptiX photoreal render behind a `PhotorealRenderer` world adapter (kernel untouched,
  rule 2), MuJoCo-state→frame over the #9 subprocess+socket scaffold. Fits our 24.04 / RTX 5080 /
  non-ROS vector-cli stack. Covers BOTH G1/Go2 photoreal VLN AND manipulation (Piper, #17/#19) — what
  the evaluated MATRiX (zsibot, MuJoCo+UE5) doesn't (quadruped-only, no public image, 22.04/Humble-only).
  MATRiX/UE5 = architecture validation, not a dependency; UE5-plugin = optional real-time upgrade only
  if Blender latency blocks. Heavy external deps remain CEO-gated.**
- **BUILD sequence (campaign.md):** (1) `PhotorealRenderer` seam: MuJoCo state → Blender subprocess →
  RGB frame, pose-aligned, behind `get_camera_observation()`, one frame end-to-end (TDD). (2) photoreal
  scene: PolyHaven CC0 PBR furniture + photoreal room (or 3DGS scan), VLM-grounding step-change vs #9.
  (3) G1/Go2 photoreal VLN (reuse recognise→navigate on photoreal frames). (4) manipulation: Piper
  grasp driven by photoreal perception. vector-cli only + screenshots; real VLM always called (rule 5).
- **BUILD R1 DONE** (`vector_os_nano/playground/photoreal/`): the co-sim wire plumbing, repo zero-dep.
  `camera.py` = pure MuJoCo→Blender camera transform (identity-of-columns; both engines share Z-up
  world + camera -Z-forward/+Y-up — TDD pins the axes so a refactor can't slip in drift). `bridge.py` =
  `BlenderBridge` client (clones the #9 subprocess+socket pattern; `blender_bin()`/`blender_available()`
  via `VECTOR_BLENDER`; PNG over base64). `server.py` = Blender-side Cycles/OptiX render server (GPL,
  subprocess-isolated — repo venv never imports `bpy`). 12 tests green (pure pose + fake-server protocol
  + Blender-gated e2e). End-to-end proven on the box: pose → bridge → **OPTIX RTX 5080** render (640×480
  @64 = 1085 ms) of a photoreal CC0 armchair → real Qwen-VL grounds **chair 0.95 + correctly rejects
  sofa** — same fidelity as R3, now through the production seam. Evidence: `~/sandbox/c10-substrate-spike/
  r1_e2e_armchair.png`. NOT yet done (R2): the `PhotorealRenderer` adapter that reads a live MuJoCo
  `(model,data)` (cam pose + body→asset scene spec) behind `get_camera_observation()`, + the photoreal
  multi-asset room. (Note: live qwen-VL hit a transient upstream 429 mid-round — called for real, failed
  loud, succeeded on retry; never faked.)
- **BUILD R2 DONE** (merged via DQ-17 f282180): the `PhotorealRenderer` world adapter + scene builder. `scene.py` =
  pure `build_room_scene_spec(placements, asset_map, ...)` → Blender scene dict that SUBSTITUTES photoreal
  CC0 assets for the toy meshes at the room's visible centres (unmapped labels skipped — partial libraries
  are valid). `camera.py` gains `look_at_camera_matrix` (build demo/test poses in MuJoCo convention).
  `renderer.py` = `PhotorealRenderer.observe(model, data)` reads the LIVE `g1_head_cam` pose
  (`cam_xpos`/`cam_xmat`/`cam_fovy`) and renders the photoreal frame from EXACTLY that pose → returns the
  `{rgb, cam_pos, cam_mat, fovy}` dict shape recognise→navigate already consumes. 18 playground tests green
  (pure scene + look-at + fake-bridge renderer asserting matrix_world == MuJoCo pose, no drift end-to-end;
  uses a tiny hand-built MJCF, no Blender/network). E2E on the box: a real MuJoCo first-person `g1_head_cam`
  pose → adapter → OPTIX 640×480@64 (~1.08 s) of the photoreal armchair room → real Qwen-VL **chair 0.95**.
  Evidence `~/sandbox/c10-substrate-spike/r2_room_photoreal.png`. Honest: the single-seat armchair also
  grounds as **sofa 0.9** (genuine VLM ambiguity on one upholstered seat — a distinct sofa asset
  disambiguates; asset-library expansion is later).
- **BUILD R3 DONE** (merged via DQ-17 f282180): photoreal wired into `mujoco_g1.get_camera_observation` as a HYBRID —
  rgb=Blender(OptiX), depth/pose=MuJoCo, one camera frame (the Blender render runs from the exact pose
  MuJoCo rendered the depth at, and over a socket so it's safe off the control thread, unlike MuJoCo's GL
  Renderer). Env-gated `photoreal` flag (default OFF → furnished-room behaviour byte-identical, rule 2/9);
  `boot_g1_agent` enables it on `g1_room_vlm` when `VECTOR_G1_PHOTOREAL` is set. Lazy `BlenderBridge`
  (injectable for tests), closed in `disconnect`; fails loud if requested with no Blender; assets from
  `VECTOR_PHOTOREAL_ASSETS` (heavy CC0 assets never vendored — unmapped targets skipped). `renderer.py`
  gained `render_from_pose`. 22 playground tests green + full suite 1670 passed, no g1 regression. **E2E
  through the REAL g1 base** (booted room+furnished+photoreal headless): `get_camera_observation` returns
  `{rgb(640×480 Blender), rgb_mujoco, depth, cam_pos, cam_mat, fovy}` and the real Qwen-VL grounds **chair
  0.9** on the photoreal frame. Evidence `~/sandbox/c10-substrate-spike/r3_g1_photoreal_obs.png` (+ the
  MuJoCo frame side-by-side).
- **BUILD R5 — vector-cli photoreal ACCEPTANCE PASSED** (owner-requested): live REPL,
  `vector-cli --scenario g1_room_vlm` + `VECTOR_G1_PHOTOREAL=1` (boot banner shows "[PHOTOREAL co-sim:
  Blender/OptiX]"). NL command `找到椅子并走过去` → VGG planner → `recognize_navigate` →
  **[PASS] verify `visited('chair')`, 1/1 verified (21.1s)** — the real VLM grounded the chair on the
  offscreen photoreal Blender frame, depth-at-bbox located it, `navigate_to` drove there, and the
  DETERMINISTIC verify confirmed arrival at the chair's GT coords (rule 5, no teleport). Live viewer
  screenshot shows the G1 standing AT the chair: `~/sandbox/c10-substrate-spike/r5_vcli_viewer.png`. The
  hybrid co-sim is confirmed in the real CLI: physics+viewer=MuJoCo, perception=photoreal Blender. Clean
  teardown (Blender subprocess closed on quit; exit segfault is the known tolerated GL-teardown 139).
  **builds #1–#3 DONE + CLI-accepted.** Remaining: scene-quality polish (per-asset yaw so the chair faces
  the camera, walls, sofa/plant CC0 assets to disambiguate) — optional; and **build #4 = Piper
  manipulation** (reuse #17/#19 MuJoCoPiper + PickTopDownSkill driven by photoreal perception).
- **BUILD R6 DONE — photoreal co-sim generalized to the Go2/Piper embodiment** (merged via DQ-17 f282180). Extracted
  the furnished-room co-sim wiring into a shared factory `playground/photoreal/cosim.py`
  (`furnished_room_renderer` + `furnished_room_asset_map`) so G1 and Go2 share ONE asset-mapping +
  bridge-lifecycle (rule 7, world-agnostic); refactored g1's `_ensure_photoreal_renderer` onto it (g1
  tests still green). `MuJoCoGo2` gained the same env-gated `photoreal` flag: `get_camera_frame` returns a
  Blender render from the resolved head cam's (`d435_rgb`/`RECOG_CAM`) LIVE pose; lazy bridge closed on
  disconnect; `go2_runtime` enables it on `VECTOR_G1_PHOTOREAL`/`VECTOR_GO2_PHOTOREAL`. 28 playground tests
  green + suite 1676 passed, no go2 regression. E2E through the real go2 base (headless furnished+photoreal):
  `get_camera_frame` → Blender 640×480 → real Qwen-VL **chair 0.9** (`r6_go2_photoreal.png`). This is the
  structural enabler for **build #4**: the Go2 carries BOTH the head camera and the Piper arm, so the pick
  skills' perception (autodetect/recognise) now grounds on photoreal frames.
- **BUILD R7 DONE — photoreal PICK-scene rendering** (merged via DQ-17 f282180). The pick scene is the apartment
  (table at x=11 + 3 graspable `pickable_*` cylinders: blue/green bottle, red can), a different world from
  the furnished VLM room. Added: `scene.py` `objects` primitive passthrough; `server.py` renders
  cylinder/box primitives (Blender, coloured — no mesh asset); `cosim.py` `build_pick_scene_spec` +
  generic `scene_renderer`; `MuJoCoGo2._ensure_photoreal_renderer` now branches (furnished→room,
  else→pick scene built from LIVE `pickable_*` body poses + colours/sizes read from the model, no
  hardcoding) via new `_pick_objects`/`_pick_table`. 29 playground tests green + suite 1676 passed (the
  3 deepseek reds + 1 habitat ticketed-drive TIMING flake that PASSES in isolation and is outside the
  changed files — not an R7 regression). E2E through the real go2 pick base (headless, photoreal):
  `_pick_objects` correctly reads the 3 live cylinders, `get_camera_frame` renders them photoreal, and
  the real Qwen-VL grounds **"red can" 0.9** (`r7_go2pick_photoreal.png`). Honest: generic "can"/"bottle"
  → `[]` — the simple primitives ground by COLOUR (the R2 toy-primitive flakiness); photoreal can/bottle
  MESH assets would harden it. This proves the photoreal PERCEPTION substrate for manipulation. NEXT (R8):
  close the loop — `vector-cli` go2_piper "认出红色的罐子→抓起来" with the grasp target PERCEPTION-derived
  (recognise→pick, not registered GT) + verify grasped + screenshot; optional photoreal object meshes.
- **First post-approval task:** prune the superseded MuJoCo-VLM-render perception code from #9 (kept
  for now — tested + interconnected; the world-agnostic builder / recognise→navigate / target_locate
  geometry are reused on the new substrate).

**Campaign #9 outcome (MuJoCo furnished-room VLN, feat/playground-vln) — sound engineering, capped by
the perception substrate:**
- World-agnostic furnished-room builder (one builder, two embodiments — g1 + go2; rule #2/#7).
- Real Qwen-VL furniture recognition on the MuJoCo render (both embodiments) — works, but flaky on
  MuJoCo's non-photoreal images (the ceiling above).
- Reliable arrival via **recognise → depth-at-bbox locate → navigate_to** (g1 headless 3/3) after
  visual-servoing proved flaky (tricky Cases 21–23).
- g1 integrated into vector-cli (`start_simulation(sim_type='g1')`, "启动g1"); live viewer shows the
  furnished room; in-REPL g1 walks via a daemon control thread (Cases 22/24). Suite 1648 green.

## Campaign history (one line each; details in git log + loop journal)

- **#2** full-stack居住世界: house world (ReplicaCAD) + 50Hz cmd_vel + real nav stack + full VLN 5/5.
- **#3** kernel refactor: false-PASS family killed (pre_satisfied / evidence-gate / single-source verify).
- **#4** robustness + LLM param-binding + E2E GUI smoke + N6 gait PROBE.
- **#5** G1 real gait (unitree_rl_gym pretrained policy, MuJoCo, zero new deps).
- **#6** G1 closed-loop waypoint navigate (obstacle-aware vgraph; euclidean geodesic on flat).
- **#7** visibility-graph obstacle planner (pure geometry); batches folded into #8.
- **#8** MuJoCo furnished-physics VLN (DQ-10=A): gait+collision+avoidance+lidar/occupancy/camera+
  explore+colour-recognition→go, one vector-cli command, GUI-verified. Merged master (DQ-4).
- **#9** real VLM semantic recognition + recognise→navigate + double-embodiment + g1 vector-cli
  integration (this campaign — see Current state).

## Run / verify

Tests: see CLAUDE.md "Build / test" (canonical chunked command, expected environmental reds:
3 deepseek `.env` + level71 GL segfault — always `--deselect` it; exit 139 + full summary = green).
Quirk: go2 sim load rewrites `mjcf/go2/scene_room_piper.xml` abs paths — `git checkout` it before
committing. Live validation: real CLI + deepseek headless first; GUI/timing behaviours are
owner-window checks (tmux real vector-cli + screenshot) — never claimed verified headless.

## Pointers

- Rules + read order: [../CLAUDE.md](../CLAUDE.md)
- Design: [ARCHITECTURE.md](ARCHITECTURE.md) · Hidden bugs: [tricky-bugs.md](tricky-bugs.md)
- Subsystems: [cli-tool-system.md](cli-tool-system.md) · [skill-protocol.md](skill-protocol.md) · [sim-dev-guide.md](sim-dev-guide.md)
- ADRs: [ADR-006](architecture-decisions/ADR-006-agent-kernel-world-plugin.md) ·
  [ADR-007](architecture-decisions/ADR-007-closed-loop-controller.md) ·
  [ADR-008](architecture-decisions/ADR-008-playground-parallel-track.md) ·
  [ADR-009](architecture-decisions/ADR-009-third-world-simulator-selection.md)
- Plans: none active. Open CEO gate: campaign #10 substrate selection (decision-queue.md).
- Superseded docs/STATUS detail live in git history (`git log --all -- <path>`). No working-tree archive.

## Autonomous loop

Owner-away iterations are campaign-driven and live OUTSIDE the repo:
`~/.vector-nano-loop/{constitution,campaign,journal,next-prompt,decision-queue}.md`.
Start with `/loop` + the constitution prompt (constitution.md = fixed state machine; campaign.md =
current milestones). Past campaign journals are archived as `journal-cN-DONE.md`.
